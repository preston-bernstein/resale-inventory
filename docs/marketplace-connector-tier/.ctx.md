# Context Snapshot: Marketplace Connector Tier

## Project shape
app
components
data
docs
eslint.config.mjs
lib
middleware.ts
next.config.ts
package-lock.json
package.json
playwright.config.ts
postcss.config.mjs
public
README.md
stryker.conf.json
tests
tsconfig.json
vitest.config.ts

## CLAUDE.md
none

## CONTEXT.md
none

## ADRs (titles only)
none (no docs/adr/ directory)

## Notes for downstream skills
- Builds directly on `docs/reseller-multi-tenant-foundation/` (tenants, `platform_connections`, consent, kill-switch) — already merged to main (PR #8, commit `cad369a`).
- Real existing function/table names this feature must call, not paraphrase: `hasValidConsent`/`recordConsent`/`revokeConsent` (`lib/consent.ts`); `createConnection`/`getConnection`/`getDecryptedCredential`/`rotateCredential`/`recordSuspensionSignal`/`reactivateConnection` (`lib/connections.ts`); `encryptCredential`/`decryptCredential` (`lib/credentialCrypto.ts`); `SUPPORTED_PLATFORMS` (`lib/constants.ts`, 8 entries — requirements.md's "7 platforms" phrasing is a known off-by-one, plan.md builds all 8 per the constraints section).
- No live third-party credentials exist for any platform. eBay gets a real Sandbox-tested connector; Etsy gets a real live-but-draft-only connector (no Etsy sandbox exists); Amazon and the 5 browser-automation platforms (Poshmark/Depop/Mercari/Vinted/Grailed) ship inert/dry-run-by-default.
- `.claude/skills/resale-inventory-architecture-contract/SKILL.md` holds core invariants (integer-cents money, UUIDv4 PKs, no ALTER CHECK, additive migrations only).
