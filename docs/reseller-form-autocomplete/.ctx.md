# Context Snapshot: Constrained Brand, Size, and ISBN Fields on Add-Item Forms

## Project shape
app, components, data, docs, eslint.config.mjs, lib, middleware.ts, next.config.ts, package-lock.json, package.json, playwright.config.ts, playwright.deployed.config.ts, postcss.config.mjs, public, README.md, stryker.conf.json, tests, tsconfig.json, vitest.config.ts

## CLAUDE.md
none

## CONTEXT.md
none

## ADRs (titles only)
none (repo has no docs/adr directory)

## Notes for downstream skills
- Repo already has live ISBN lookup (`lib/isbn.ts`, `app/api/isbn/[isbn]/route.ts`) and history-based `<datalist>` suggestions (`lib/suggestions.ts`, `app/api/items/suggestions/route.ts`) — this feature is a targeted tightening, not greenfield.
- Multi-tenant scoping (`tenant_id`, `requireTenant`) is already established throughout the codebase — new tables/routes follow that pattern.
- Migration convention: `data/migrations/00N_*.sql` gated on `PRAGMA user_version` in `lib/db.ts`'s `VERSIONED_MIGRATIONS` array, currently through `010_poshmark_pacing.sql`.
- Sacred-DB rule: never delete/recreate `data/inventory.db`; tests always target `BOOKSELLER_DB_PATH`/`BOOKSELLER_PHOTOS_PATH` scratch DBs.
- No UI-library dependency exists or is being added — hand-rolled Tailwind + `FieldError` convention throughout.
